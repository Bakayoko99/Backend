const express = require('express');
const app = express();


var port = 8001;

app.get('/', (req, res) => {




})





app.listen(port, () => {
    console.log('Server started on port: ' + port);
  });
